server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Optional: Proxy API requests if you wanted Nginx to handle it, 
    # but we'll use env vars in React to point to the backend URL.
}
