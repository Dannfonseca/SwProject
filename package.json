{
    "name": "swscraping-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "concurrently \"cd backend && bun src/index.ts\" \"cd frontend && npm run dev\"",
        "install:all": "cd backend && bun install && cd frontend && npm install"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}